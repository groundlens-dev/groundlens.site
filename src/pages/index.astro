---
const title = "GroundLens — Verification infrastructure for AI systems and agents";
const description = "GroundLens turns observable AI execution into policy-governed, independently verifiable evidence. Verify claims and actions, enforce execution policies, and produce signed records for audit.";
const github = "https://github.com/groundlens-dev/groundlens";
const docs = "https://github.com/groundlens-dev/groundlens/tree/main/docs";
const examples = "https://github.com/groundlens-dev/groundlens/tree/main/examples";
const runExample = "https://github.com/groundlens-dev/groundlens/tree/main/examples/run";
const policies = "https://github.com/groundlens-dev/groundlens/tree/main/policies";
const security = "https://github.com/groundlens-dev/groundlens/blob/main/SECURITY.md";
const roadmap = "https://github.com/groundlens-dev/groundlens/blob/main/ROADMAP.md";
const site = "https://groundlens.dev/";
---

<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{title}</title>
  <meta name="description" content={description} />
  <link rel="canonical" href={site} />
  <meta name="theme-color" content="#07090c" />
  <meta property="og:type" content="website" />
  <meta property="og:url" content={site} />
  <meta property="og:title" content={title} />
  <meta property="og:description" content={description} />
  <meta property="og:image" content={`${site}Groundlens-03.png`} />
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content={title} />
  <meta name="twitter:description" content={description} />
  <meta name="twitter:image" content={`${site}Groundlens-03.png`} />
  <link rel="icon" href="/Favicon.svg" />
  <script type="application/ld+json" set:html={JSON.stringify({
    "@context": "https://schema.org",
    "@type": "SoftwareApplication",
    name: "GroundLens",
    applicationCategory: "DeveloperApplication",
    operatingSystem: "Linux, macOS, Windows",
    url: site,
    codeRepository: github,
    license: "https://www.apache.org/licenses/LICENSE-2.0",
    description,
    offers: { "@type": "Offer", price: "0", priceCurrency: "USD" }
  })} />
</head>
<body>
  <div class="site-shell" id="top">
    <div class="noise" aria-hidden="true"></div>

    <header class="nav shell">
      <a class="brand" href="#top" aria-label="GroundLens home">
        <span class="brand-mark"><img src="/Groundlens-03.png" alt="" width="28" height="28" /></span>
        <span>GroundLens</span>
      </a>
      <nav class="nav-links" aria-label="Primary navigation">
        <a href="#why">Why</a>
        <a href="#runtime">Runtime</a>
        <a href="#verifiers">Verifiers</a>
        <a href="#evidence">Evidence</a>
        <a href={github}>GitHub <span>↗</span></a>
      </nav>
      <a class="nav-cta" href={github}>View repository <span>↗</span></a>
    </header>

    <main>
      <section class="hero shell">
        <div class="hero-copy">
          <div class="eyebrow"><span class="pulse"></span>OPEN SOURCE · APACHE-2.0 · LOCAL-FIRST</div>
          <h1>Verification infrastructure for <span>AI systems and agents.</span></h1>
          <p class="hero-lead">
            GroundLens turns observable AI execution into <strong>policy-governed, independently verifiable evidence.</strong>
          </p>
          <p class="hero-body">
            Verify claims, tool calls, actions and outcomes. Record what happened, what was checked, which policy applied,
            and why a step was allowed, reviewed or denied.
          </p>
          <div class="hero-actions">
            <a class="button button-primary" href={github}>Explore GroundLens <span>↗</span></a>
            <a class="button button-secondary" href="#how">See how it works</a>
          </div>
          <div class="hero-facts">
            <span>Rust engine</span>
            <span>Python bindings</span>
            <span>MCP-ready</span>
            <span>Offline verification</span>
            <span>Signed records</span>
          </div>
        </div>

        <div class="hero-panel" aria-label="Illustration of a verified AI execution">
          <div class="trace-window">
            <div class="window-bar">
              <div class="window-dots"><i></i><i></i><i></i></div>
              <span class="window-title">agent.run / verification trace</span>
              <span class="window-state">SEALED</span>
            </div>
            <div class="trace-body">
              <div class="trace-line dim"><span>01</span><b>RUN</b><em>invoice-agent</em><small>start</small></div>
              <div class="trace-line"><span>02</span><b>MODEL</b><em>response.generate</em><small>ok</small></div>
              <div class="trace-line"><span>03</span><b>RETRIEVAL</b><em>invoice.pdf#p1</em><small>hash</small></div>
              <div class="trace-line accent"><span>04</span><b>VERIFY</b><em>numeric + nli</em><small>evidence</small></div>
              <div class="trace-line"><span>05</span><b>TOOL</b><em>invoice.lookup</em><small class="allow">ALLOW</small></div>
              <div class="trace-line"><span>06</span><b>ACTION</b><em>customer.notify</em><small class="review">REVIEW</small></div>
            </div>
            <div class="trace-footer">
              <div>
                <span class="mono-label">SIGNED EVIDENCE RECORD</span>
                <strong>rec_7f2c…91ae</strong>
              </div>
              <div class="verified"><span>✓</span> VERIFIED OFFLINE</div>
            </div>
          </div>
          <div class="hero-panel-caption">
            <span>Execution</span><i></i><span>Evidence</span><i></i><span>Policy</span><i></i><span>Decision</span><i></i><span>Record</span>
          </div>
        </div>
      </section>

      <section class="statement-strip">
        <div class="shell statement-grid">
          <div>
            <span class="micro">THE GAP</span>
            <strong>AI observability tells you what happened. It does not, by itself, prove that what happened was permitted or adequately verified.</strong>
          </div>
          <div>
            <span class="micro">THE LAYER</span>
            <strong>GroundLens adds verification, policy and evidence without taking ownership of the model, agent or data plane.</strong>
          </div>
          <div>
            <span class="micro">THE OUTPUT</span>
            <strong>A portable record a reviewer, operator or auditor can independently verify later.</strong>
          </div>
        </div>
      </section>

      <section id="why" class="section shell">
        <div class="section-intro">
            <span class="kicker">Why GroundLens</span>
          <h2>The hard part is not another score. <span>It is being able to show what was checked.</span></h2>
          <p>
            AI systems increasingly move from generating text to taking actions. A production verification layer therefore needs
            to handle both the content of an answer and the execution that produced it — with enough evidence to make the result reviewable.
          </p>
        </div>

        <div class="problem-grid">
          <article class="problem-card muted-card">
            <div class="card-top"><span>01</span><span>OBSERVABILITY</span></div>
            <h3>“The tool was called.”</h3>
            <p>Tracing systems can record the request, response and timing. Useful, but a trace alone does not explain which rule governed the action or whether a control was honoured.</p>
            <div class="card-caption">event log ≠ verification evidence</div>
          </article>
          <article class="problem-card muted-card">
            <div class="card-top"><span>02</span><span>GUARDRAILS</span></div>
            <h3>“The action was blocked.”</h3>
            <p>A runtime gate can stop a step at the moment it happens. GroundLens is designed to keep the policy decision and its supporting evidence as a durable artefact as well.</p>
            <div class="card-caption">control ≠ portable proof</div>
          </article>
          <article class="problem-card emphasis-card">
            <div class="card-top"><span>03</span><span>GROUNDLENS</span></div>
            <h3>“Here is what happened — and the proof.”</h3>
            <p>Every verification can identify the verifiers that ran, the evidence they produced, the policy that interpreted it, the resulting decision and the cryptographic record that binds it together.</p>
            <div class="card-caption">verification = evidence + policy + provenance</div>
          </article>
        </div>
      </section>

      <section id="runtime" class="section section-dark">
        <div class="shell">
          <div class="section-intro narrow">
            <span class="kicker">Execution Runtime</span>
            <h2>One contract from <span>the first token to the final side effect.</span></h2>
            <p>
              The unit is the execution: an ordered sequence of steps an AI system or agent takes. A single answer is simply the smallest
              execution — one run with one claim.
            </p>
          </div>

          <div class="execution-layout">
            <div class="execution-rail">
              <div class="rail-item" data-symbol="✦"><span>01</span><strong>Model call</strong><small>generation / reasoning</small></div>
              <div class="rail-item" data-symbol="⌕"><span>02</span><strong>Retrieval</strong><small>sources / context</small></div>
              <div class="rail-item highlight" data-symbol="⌘"><span>03</span><strong>Tool request</strong><small>arguments / identity / scope</small></div>
              <div class="rail-item highlight" data-symbol="↳"><span>04</span><strong>Tool result</strong><small>returned data / outcome</small></div>
              <div class="rail-item highlight" data-symbol="→"><span>05</span><strong>Action</strong><small>side effects / risk</small></div>
              <div class="rail-item" data-symbol="✓"><span>06</span><strong>Human approval</strong><small>review / escalation</small></div>
            </div>

            <div class="runtime-explainer">
              <div class="runtime-feature">
                <div class="feature-number">A</div>
                <div>
                  <h3>Observe beside the system</h3>
                  <p>GroundLens sits next to the AI system rather than requiring access to model weights, prompts or internal architecture. The runtime works from the observable execution surface.</p>
                </div>
              </div>
              <div class="runtime-feature">
                <div class="feature-number">B</div>
                <div>
                  <h3>Verify before and after consequential steps</h3>
                  <p>Claims can be checked with composable verifiers. Tool calls and actions can be evaluated by an ordered execution policy that returns <code>ALLOW</code>, <code>REVIEW</code> or <code>DENY</code>.</p>
                </div>
              </div>
              <div class="runtime-feature">
                <div class="feature-number">C</div>
                <div>
                  <h3>Keep the execution auditable</h3>
                  <p>Each step can become an event in a hash-linked run log. After the run, GroundLens can audit the entire execution against the policy and record breaches in the signed result.</p>
                </div>
              </div>
              <div class="runtime-callout">
                <span class="callout-mark">↳</span>
                <div><strong>Built for systems that already emit traces.</strong><span>MCP JSON-RPC messages can be ingested as an execution trace, with hashes recorded instead of raw tool arguments and results.</span></div>
              </div>
            </div>
          </div>
        </div>
      </section>

      <section id="how" class="section shell">
        <div class="section-intro centered">
            <span class="kicker">How It Works</span>
          <h2>A verifier produces evidence. <span>A policy makes the decision.</span></h2>
          <p>
            GroundLens deliberately separates measurement from decision-making. The verifier says what it observed. The policy determines what that evidence means for the deployment.
          </p>
        </div>

        <div class="flow-grid">
          <article class="flow-card">
            <div class="flow-index">01</div>
            <h3>Verify</h3>
            <p>Run one or more verifiers against a claim, source, tool call or execution event.</p>
            <div class="flow-code">input → evidence</div>
          </article>
          <div class="flow-arrow">→</div>
          <article class="flow-card active">
            <div class="flow-index">02</div>
            <h3>Interpret</h3>
            <p>A versioned policy interprets the evidence according to the risk rules of the deployment.</p>
            <div class="flow-code">evidence → decision</div>
          </article>
          <div class="flow-arrow">→</div>
          <article class="flow-card">
            <div class="flow-index">03</div>
            <h3>Record</h3>
            <p>The evidence, policy, decision and provenance are sealed into a signed record.</p>
            <div class="flow-code">decision → evidence record</div>
          </article>
        </div>

        <div class="decision-band">
          <div class="decision-group">
            <span class="decision-title">Content checks <small>claim and evidence results</small></span>
            <span class="decision-pill pass">PASS</span>
            <span class="decision-pill review">REVIEW</span>
            <span class="decision-pill fail">FAIL</span>
          </div>
          <div class="decision-divider"></div>
          <div class="decision-group">
            <span class="decision-title">Execution controls <small>actions and tool permissions</small></span>
            <span class="decision-pill allow">ALLOW</span>
            <span class="decision-pill review">REVIEW</span>
            <span class="decision-pill deny">DENY</span>
          </div>
        </div>
      </section>

      <section id="verifiers" class="section section-light">
        <div class="shell">
          <div class="section-intro light-intro">
            <span class="kicker">Verifiers</span>
            <h2>Compose checks instead of betting everything on <span>one detector.</span></h2>
            <p>
              GroundLens gives different verification methods the same evidence contract. Exact checks can remain exact; statistical checks can declare their reproducibility class and calibration details.
            </p>
          </div>

          <div class="verifier-table">
            <article>
              <div class="verifier-head"><span>01</span><b>NUMERIC</b></div>
              <h3>Numbers, currencies, percentages and units.</h3>
              <p>Compare quantities exactly in base units, including locale-aware parsing and legitimate unit conversions.</p>
              <span class="guarantee">EXACT</span>
            </article>
            <article>
              <div class="verifier-head"><span>02</span><b>RULES</b></div>
              <h3>Your symbolic domain checks.</h3>
              <p>Encode deterministic constraints such as date ranges, field relationships or domain-specific invariants.</p>
              <span class="guarantee">EXACT</span>
            </article>
            <article>
              <div class="verifier-head"><span>03</span><b>LEXICAL</b></div>
              <h3>Evidence at the word and source-span level.</h3>
              <p>Anchor answer text against source material using a pinned multilingual encoder and retain the evidence that produced the finding.</p>
              <span class="guarantee">REPRODUCIBLE</span>
            </article>
            <article>
              <div class="verifier-head"><span>04</span><b>NLI</b></div>
              <h3>Entailment, contradiction and neutrality.</h3>
              <p>Check statements against a source with a cross-encoder when the deployment policy requires semantic verification.</p>
              <span class="guarantee">REPRODUCIBLE</span>
            </article>
          </div>

          <div class="verifier-note">
            <span class="note-symbol">+</span>
            <div><strong>The contract is extensible.</strong><p>Semantic, geometric and LLM-judge verifiers can plug into the same verification model. Any non-deterministic component can be recorded with the model, prompt hash and settings that produced it.</p></div>
          </div>
        </div>
      </section>

      <section class="section shell policy-section">
        <div class="two-col">
          <div class="section-intro">
            <span class="kicker">Policy as Code</span>
            <h2>Your risk rules live in <span>policy, not inside the verifier.</span></h2>
            <p>
              Two policies can interpret the same evidence differently. That is intentional: risk appetite, approval requirements and enforcement belong to the deployment policy and should be versioned independently from measurement.
            </p>
            <a class="text-link" href={policies}>See the example policies <span>↗</span></a>
          </div>

          <div class="code-card">
            <div class="code-card-top"><span>execution-policy.yaml</span><span>VERSIONED</span></div>
            <pre><code><span class="yaml-key">id</span>: <span class="yaml-string">eu_high_risk_v1</span>
<span class="yaml-key">rules</span>:
  - <span class="yaml-key">id</span>: <span class="yaml-string">no-shell</span>
    <span class="yaml-key">match</span>: <span class="yaml-string">tool</span>
    <span class="yaml-key">name</span>: <span class="yaml-string">shell.exec</span>
    <span class="yaml-key">effect</span>: <span class="yaml-deny">DENY</span>

  - <span class="yaml-key">id</span>: <span class="yaml-string">human-for-high-risk</span>
    <span class="yaml-key">match</span>: <span class="yaml-string">risk_at_least</span>
    <span class="yaml-key">risk</span>: <span class="yaml-string">high</span>
    <span class="yaml-key">effect</span>: <span class="yaml-review">REVIEW</span>

<span class="yaml-key">default</span>: <span class="yaml-allow">ALLOW</span></code></pre>
            <div class="code-card-foot"><span>ordered rules · first match wins</span><span>pure rule matching</span></div>
          </div>
        </div>
      </section>

      <section id="evidence" class="section section-dark evidence-section">
        <div class="shell two-col evidence-layout">
          <div class="section-intro">
            <span class="kicker">Evidence Records</span>
            <h2>Trust is not a score. <span>It is evidence with provenance.</span></h2>
            <p>
              The final artefact carries enough information to reconstruct what participated in the decision: inputs, verifier identities and hashes, evidence, policy and policy hash, decision, regulatory mapping and the previous record hash.
            </p>
            <div class="evidence-points">
              <div><span>01</span><strong>Hash-linked</strong><p>Each record carries the hash of the previous record in the log.</p></div>
              <div><span>02</span><strong>Signed</strong><p>Ed25519 seals the record so a verifier can detect changes later.</p></div>
              <div><span>03</span><strong>Offline-verifiable</strong><p>Verification can recompute hashes and signatures without reaching a network.</p></div>
            </div>
          </div>

          <div class="record-card">
            <div class="record-card-top"><span>evidence-record.json</span><span>SEALED</span></div>
            <pre><code><span class="j-brace">{</span>
  <span class="j-key">"record_id"</span>: <span class="j-string">"rec_350455f4…"</span>,
  <span class="j-key">"engine_version"</span>: <span class="j-string">"…"</span>,

  <span class="j-key">"inputs"</span>: <span class="j-bracket">{</span>
    <span class="j-key">"answer_hash"</span>: <span class="j-string">"sha256:…"</span>,
    <span class="j-key">"source_hash"</span>: <span class="j-string">"sha256:…"</span>
  <span class="j-bracket">}</span>,

  <span class="j-key">"verifiers"</span>: <span class="j-bracket">[</span>
    <span class="j-string">"groundlens.numeric@…"</span>,
    <span class="j-string">"groundlens.nli@…"</span>
  <span class="j-bracket">]</span>,

  <span class="j-key">"policy"</span>: <span class="j-string">"eu_high_risk_v1"</span>,
  <span class="j-key">"decision"</span>: <span class="j-review">"REVIEW"</span>,
  <span class="j-key">"regulatory_mapping"</span>: <span class="j-bracket">[</span><span class="j-string">…</span><span class="j-bracket">]</span>,
  <span class="j-key">"previous_record_hash"</span>: <span class="j-string">"sha256:…"</span>,
  <span class="j-key">"signature"</span>: <span class="j-string">"ed25519:…"</span>
<span class="j-brace">}</span></code></pre>
            <div class="record-card-foot">
              <span><i class="green-dot"></i> tamper-evident</span>
              <span>verify() · verify_chain()</span>
            </div>
          </div>
        </div>
      </section>

      <section class="section shell">
        <div class="section-intro centered">
            <span class="kicker">Designed for Controlled Environments</span>
          <h2>Verification should not become <span>another data dependency.</span></h2>
          <p>
            The runtime is deliberately local-first. Sensitive deployments can keep verification close to the system they are protecting, while the evidence artefact stays portable.
          </p>
        </div>

        <div class="environment-grid">
          <article>
            <span class="env-icon">01</span>
            <h3>No model access required</h3>
            <p>GroundLens operates on observable execution. It does not need your weights, prompts or internal architecture.</p>
          </article>
          <article>
            <span class="env-icon">02</span>
            <h3>Verification is offline</h3>
            <p>The engine and runtime have no HTTP/TLS dependency. Network access is explicit and isolated to the optional <code>bundle pull</code> operation.</p>
          </article>
          <article>
            <span class="env-icon">03</span>
            <h3>Content can stay out of the log</h3>
            <p>Execution traces can record hashes of tool arguments and results instead of persisting the raw content in the evidence record.</p>
          </article>
          <article>
            <span class="env-icon">04</span>
            <h3>Reproducibility is explicit</h3>
            <p>Exact checks are exact. Pinned statistical verifiers carry their model identity, hash and declared tolerance. Non-deterministic components can be recorded rather than hidden.</p>
          </article>
        </div>
      </section>

      <section class="section section-dark stack-section">
        <div class="shell">
          <div class="section-intro narrow">
            <span class="kicker">Fits Beside Your Stack</span>
            <h2>Keep your models, agents and observability stack. <span>Add one verification layer.</span></h2>
          </div>

          <div class="stack-diagram">
            <div class="stack-row top-stack">
              <span>LLMs</span><span>RAG</span><span>Agents</span><span>Tools</span><span>Memory</span><span>Human workflow</span>
            </div>
            <div class="stack-arrow">↓ observable execution ↓</div>
            <div class="stack-core">
              <div class="stack-core-label">GROUNDLENS</div>
              <div class="stack-core-grid">
                <div><strong>Observe</strong><small>events / traces</small></div>
                <div><strong>Verify</strong><small>claims / actions</small></div>
                <div><strong>Policy</strong><small>rules / gates</small></div>
                <div><strong>Record</strong><small>evidence / signatures</small></div>
              </div>
            </div>
            <div class="stack-arrow">↓ portable evidence ↓</div>
            <div class="stack-row bottom-stack">
              <span>Review</span><span>Audit</span><span>Compliance</span><span>Incident analysis</span><span>Independent verification</span>
            </div>
          </div>
        </div>
      </section>

      <section class="section shell usecase-section">
        <div class="two-col usecase-header">
          <div class="section-intro">
            <span class="kicker">Where It Fits</span>
            <h2>From one answer to a <span>full agent run.</span></h2>
          </div>
          <p class="usecase-lead">The same evidence model can be used at different boundaries in the lifecycle of an AI system.</p>
        </div>

        <div class="usecase-grid">
          <article>
            <div class="usecase-tag">CONTENT</div>
            <h3>RAG and grounded answers</h3>
            <p>Verify claims against retrieved evidence, combine exact and semantic checks, and preserve the evidence behind the decision.</p>
            <div class="usecase-line">answer → claims → evidence → decision</div>
          </article>
          <article>
            <div class="usecase-tag">EXECUTION</div>
            <h3>Agent tool calls and actions</h3>
            <p>Inspect the sequence of events and apply execution policy at the tool/action boundary.</p>
            <div class="usecase-line">trace → gate → action → record</div>
          </article>
          <article>
            <div class="usecase-tag">ASSURANCE</div>
            <h3>Pre-deployment verification</h3>
            <p>Run representative executions through a controlled policy and produce an evidence package before a system reaches production.</p>
            <div class="usecase-line">dataset → verification → report</div>
          </article>
          <article>
            <div class="usecase-tag">AUDIT</div>
            <h3>Independent review and replay</h3>
            <p>Hand over a signed record or JSONL chain and verify its integrity without depending on the original runtime.</p>
            <div class="usecase-line">record → verify → review</div>
          </article>
        </div>
      </section>

      <section class="section section-light technical-section">
        <div class="shell two-col">
          <div class="section-intro light-intro">
            <span class="kicker">Engineering Surface</span>
            <h2>Small core. Explicit boundaries. <span>Inspectable by design.</span></h2>
            <p>
              GroundLens keeps the verification engine independent from deployment infrastructure. The Rust workspace exposes the same core through the Python layer and the <code>glv</code> binary.
            </p>
          </div>
          <div class="technical-list">
            <div><span>RUST</span><strong>Core engine and runtime</strong><p>Verification logic, execution events, policy evaluation and record sealing live in a portable core.</p></div>
            <div><span>PYTHON</span><strong>Developer-facing API</strong><p>Use GroundLens from Python without moving the verification contract into a cloud service.</p></div>
            <div><span>CLI</span><strong>Automation-friendly execution</strong><p>Verify answers, inspect records, lint policies, pull bundles and verify chains from the command line.</p></div>
            <div><span>MCP</span><strong>Agent execution boundary</strong><p>Ingest MCP JSON-RPC traces and keep execution evidence tied to the same runtime model.</p></div>
          </div>
        </div>
      </section>

      <section class="section section-dark final-section">
        <div class="shell final-cta">
          <div class="final-mark" aria-hidden="true"><i></i><i></i><i></i></div>
          <span class="kicker">Open Source Foundation</span>
          <h2>Build AI systems that can show <span>how they were verified.</span></h2>
          <p>
            GroundLens is the verification layer underneath a broader governance stack: observe the execution, verify the relevant facts and actions, interpret them with policy, and leave evidence that can survive outside the original runtime.
          </p>
          <div class="hero-actions center-actions">
            <a class="button button-primary" href={github}>Read the source <span>↗</span></a>
            <a class="button button-secondary" href={security}>Security policy <span>↗</span></a>
          </div>
          <div class="final-meta">
            <a href={roadmap}>Roadmap ↗</a>
            <span>·</span>
            <a href={policies}>Policies ↗</a>
            <span>·</span>
            <a href={runExample}>Agent run example ↗</a>
          </div>
        </div>
      </section>
    </main>

    <footer class="footer shell">
      <span class="copyright">Copyright GroundLens, 2026</span>
      <a href={github}>GitHub ↗</a>
      <a href="https://www.apache.org/licenses/LICENSE-2.0">Apache-2.0</a>
    </footer>
  </div>
</body>
</html>

<style>
  :root {
    --bg: #07090c;
    --panel: #0c1015;
    --panel-2: #10151b;
    --panel-3: #141b22;
    --text: #f2f5f7;
    --muted: #95a0aa;
    --muted-2: #69747f;
    --muted-3: #4d5862;
    --line: rgba(255,255,255,.085);
    --line-strong: rgba(255,255,255,.13);
    --accent: #ff7a18;
    --accent-soft: rgba(255,122,24,.12);
    --green: #64d79b;
    --red: #ff6c67;
    --paper: #eef0ed;
    --paper-2: #f5f6f4;
    --ink: #10161b;
    --ink-muted: #657078;
    --max: 1220px;
  }

  * { box-sizing: border-box; }
  html { scroll-behavior: smooth; }
  body {
    margin: 0;
    background: var(--bg);
    color: var(--text);
    font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    -webkit-font-smoothing: antialiased;
    text-rendering: optimizeLegibility;
  }
  a { color: inherit; text-decoration: none; }
  code, pre, .mono-label, .kicker, .micro, .guarantee, .window-title, .window-state, .card-caption, .flow-code, .usecase-line, .quickstart-tab, .final-meta, .env-icon, .verifier-head, .stack-core-label, .yaml-key, .yaml-string, .yaml-deny, .yaml-review, .yaml-allow, .j-key, .j-string, .j-review, .j-brace, .j-bracket {
    font-family: "SFMono-Regular", Consolas, "Liberation Mono", monospace;
  }
  code { color: #ced6dc; }
  .site-shell { min-height: 100vh; overflow: clip; position: relative; }
  .noise {
    position: fixed;
    inset: 0;
    pointer-events: none;
    opacity: .035;
    z-index: 50;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 160 160' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='.92' numOctaves='2' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='.24'/%3E%3C/svg%3E");
  }
  .shell { width: min(calc(100% - 72px), var(--max)); margin: 0 auto; }

  .nav {
    min-height: 82px;
    display: grid;
    grid-template-columns: auto 1fr auto;
    align-items: center;
    gap: 26px;
    border-bottom: 1px solid var(--line);
  }
  .brand { display: inline-flex; align-items: center; gap: 10px; font-weight: 700; letter-spacing: -.02em; }
  .brand-mark { width: 29px; height: 29px; display: grid; place-items: center; }
  .brand-mark img { display: block; width: 28px; height: 28px; object-fit: contain; }
  .nav-links { display: flex; justify-content: center; gap: 28px; }
  .nav-links a { color: #8e99a4; font-size: 12px; transition: color .2s ease; }
  .nav-links a:hover, .footer a:hover, .text-link:hover { color: var(--text); }
  .nav-links a span, .nav-cta span { color: var(--muted-3); }
  .nav-cta {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 10px 13px;
    border: 1px solid var(--line-strong);
    border-radius: 6px;
    background: rgba(255,255,255,.02);
    font-size: 11px;
    color: #d1d8de;
    transition: border-color .2s ease, background .2s ease;
  }
  .nav-cta:hover { border-color: rgba(255,122,24,.45); background: rgba(255,122,24,.045); }

  .hero { min-height: 730px; display: grid; grid-template-columns: 1.03fr .97fr; gap: 74px; align-items: center; padding-top: 72px; padding-bottom: 76px; }
  .hero-copy { max-width: 700px; }
  .eyebrow, .kicker, .micro { letter-spacing: .14em; text-transform: uppercase; }
  .eyebrow { display: flex; align-items: center; gap: 8px; color: #7f8a95; font-size: 9px; font-family: "SFMono-Regular", Consolas, monospace; }
  .pulse { width: 6px; height: 6px; border-radius: 50%; background: var(--green); box-shadow: 0 0 0 4px rgba(100,215,155,.08); }
  h1 { margin: 17px 0 26px; font-size: clamp(3.8rem, 6.8vw, 6.7rem); line-height: .94; letter-spacing: -.065em; font-weight: 630; }
  h1 span, h2 span { color: #8c98a4; }
  .hero-lead { max-width: 650px; margin: 0 0 15px; color: #d7dde2; font-size: 20px; line-height: 1.55; letter-spacing: -.01em; }
  .hero-lead strong { color: #f1f4f6; font-weight: 580; }
  .hero-body { max-width: 640px; margin: 0; color: var(--muted); font-size: 14px; line-height: 1.75; }
  .hero-actions { display: flex; gap: 10px; margin-top: 30px; }
  .button { display: inline-flex; align-items: center; justify-content: center; gap: 9px; min-height: 44px; padding: 0 17px; border-radius: 6px; font-size: 12px; font-weight: 600; transition: transform .2s ease, border-color .2s ease, background .2s ease; }
  .button:hover { transform: translateY(-1px); }
  .button-primary { color: #101419; background: #f3f5f6; border: 1px solid #f3f5f6; }
  .button-primary:hover { background: white; }
  .button-secondary { color: #c5cdd3; border: 1px solid var(--line-strong); background: rgba(255,255,255,.018); }
  .button-secondary:hover { border-color: rgba(255,122,24,.38); background: rgba(255,122,24,.035); }
  .hero-facts { display: flex; flex-wrap: wrap; gap: 8px 14px; margin-top: 24px; color: var(--muted-3); font-size: 10px; font-family: "SFMono-Regular", Consolas, monospace; }
  .hero-facts span + span::before { content: "·"; margin-right: 14px; color: #3f4a54; }

  .hero-panel { width: 100%; }
  .trace-window { border: 1px solid #2a333d; border-radius: 9px; background: #090c10; box-shadow: 0 40px 100px rgba(0,0,0,.35); overflow: hidden; transform: rotate(.15deg); }
  .window-bar, .trace-footer, .record-card-top, .record-card-foot, .code-card-top, .code-card-foot, .quickstart-tab { display: flex; align-items: center; justify-content: space-between; }
  .window-bar { min-height: 39px; padding: 0 14px; border-bottom: 1px solid var(--line); }
  .window-dots { display: flex; gap: 5px; }
  .window-dots i { width: 5px; height: 5px; border-radius: 50%; background: #37414b; }
  .window-title, .window-state { color: #58636d; font-size: 8px; letter-spacing: .08em; }
  .window-state { color: var(--green); }
  .trace-body { padding: 14px 14px 11px; }
  .trace-line { display: grid; grid-template-columns: 28px 82px 1fr auto; gap: 10px; align-items: center; min-height: 42px; border-bottom: 1px solid rgba(255,255,255,.045); color: #bac4cc; font-family: "SFMono-Regular", Consolas, monospace; font-size: 10px; }
  .trace-line:last-child { border-bottom: 0; }
  .trace-line > span { color: #46515b; }
  .trace-line b { font-size: 8px; letter-spacing: .07em; color: #6b7781; font-weight: 500; }
  .trace-line em { font-style: normal; color: #b9c3ca; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .trace-line small { color: #55616c; font-size: 8px; text-transform: uppercase; }
  .trace-line.dim { opacity: .55; }
  .trace-line.accent { margin: 3px -4px; padding: 0 4px; border: 1px solid rgba(255,122,24,.18); border-radius: 4px; background: rgba(255,122,24,.035); }
  .allow { color: var(--green) !important; }
  .review { color: #f3bd69 !important; }
  .trace-footer { padding: 15px 17px; border-top: 1px solid var(--line); background: #0d1116; }
  .trace-footer strong { display: block; margin-top: 4px; color: #cdd4da; font-size: 11px; font-family: "SFMono-Regular", Consolas, monospace; font-weight: 500; }
  .mono-label { display: block; color: #53606b; font-size: 7px; letter-spacing: .12em; }
  .verified { color: var(--green); font-family: "SFMono-Regular", Consolas, monospace; font-size: 8px; letter-spacing: .07em; }
  .verified span { margin-right: 5px; }
  .hero-panel-caption { display: flex; align-items: center; gap: 8px; margin-top: 15px; color: #56616b; font: 8px "SFMono-Regular", Consolas, monospace; text-transform: uppercase; letter-spacing: .08em; }
  .hero-panel-caption i { display: block; width: 12px; height: 1px; background: #323b44; }

  .statement-strip { border-top: 1px solid var(--line); border-bottom: 1px solid var(--line); background: rgba(255,255,255,.013); }
  .statement-grid { display: grid; grid-template-columns: repeat(3, 1fr); }
  .statement-grid > div { min-height: 138px; padding: 31px 30px; border-right: 1px solid var(--line); }
  .statement-grid > div:first-child { padding-left: 0; }
  .statement-grid > div:last-child { border-right: 0; padding-right: 0; }
  .micro { display: block; margin-bottom: 10px; color: var(--accent); font-size: 8px; font-weight: 600; }
  .statement-grid strong { display: block; max-width: 35ch; color: #bcc5cc; font-size: 13px; line-height: 1.55; font-weight: 500; }

  .section { padding-top: 130px; padding-bottom: 130px; }
  .section-dark { background: radial-gradient(680px 400px at 50% 45%, rgba(255,122,24,.05), transparent 70%), #090c10; border-top: 1px solid var(--line); border-bottom: 1px solid var(--line); }
  .section-light { background: var(--paper); color: var(--ink); }
  .section-intro { max-width: 760px; margin-bottom: 58px; }
  .section-intro.narrow { max-width: 820px; }
  .section-intro.centered { margin-left: auto; margin-right: auto; text-align: center; }
  .section-intro.light-intro p { color: var(--ink-muted); }
  .kicker { color: #68747e; font-size: 8px; font-weight: 600; }
  .section-dark .kicker, .final-section .kicker { color: #69757f; }
  h2 { margin: 12px 0 18px; font-size: clamp(2.6rem, 5vw, 4.8rem); line-height: .99; letter-spacing: -.058em; font-weight: 620; }
  .section-intro p { margin: 0; max-width: 69ch; color: var(--muted); font-size: 15px; line-height: 1.78; }

  .problem-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1px; border: 1px solid var(--line); background: var(--line); }
  .problem-card { min-height: 330px; padding: 28px; background: #0b0f13; }
  .problem-card.emphasis-card { background: linear-gradient(135deg, #14191f, #0d1116); }
  .card-top { display: flex; justify-content: space-between; color: #57636e; font: 8px "SFMono-Regular", Consolas, monospace; letter-spacing: .12em; }
  .emphasis-card .card-top span:last-child { color: var(--accent); }
  .problem-card h3 { margin: 53px 0 11px; font-size: 20px; letter-spacing: -.025em; }
  .problem-card p { margin: 0; color: var(--muted); font-size: 13px; line-height: 1.72; }
  .card-caption { margin-top: 48px; color: #4a5661; font-size: 8px; letter-spacing: .09em; }
  .emphasis-card .card-caption { color: #8e6950; }

  .execution-layout { display: grid; grid-template-columns: .8fr 1.2fr; gap: 105px; align-items: start; }
  .execution-rail { position: relative; padding-left: 3px; }
  .execution-rail::before { content: ""; position: absolute; left: 18px; top: 17px; bottom: 17px; width: 1px; background: linear-gradient(var(--line-strong), var(--line)); }
  .rail-item { position: relative; display: grid; grid-template-columns: 32px 1fr; gap: 12px; padding: 18px 18px 18px 2px; }
  .rail-item > span { z-index: 1; display: grid; place-items: center; width: 34px; height: 34px; border: 1px solid #2c3640; border-radius: 50%; background: #0a0d11; color: #68747e; font: 8px "SFMono-Regular", Consolas, monospace; }
  .rail-item strong { padding-top: 2px; font-size: 14px; font-weight: 600; }
  .rail-item small { grid-column: 2; margin-top: -7px; color: #616d78; font: 9px "SFMono-Regular", Consolas, monospace; }
  .rail-item.highlight > span { border-color: rgba(255,122,24,.34); color: var(--accent); }
  .runtime-explainer { display: grid; gap: 13px; }
  .runtime-feature { display: grid; grid-template-columns: 42px 1fr; gap: 18px; padding: 25px 0; border-bottom: 1px solid var(--line); }
  .feature-number { color: #4c5964; font: 10px "SFMono-Regular", Consolas, monospace; padding-top: 3px; }
  .runtime-feature h3 { margin: 0 0 7px; font-size: 16px; font-weight: 600; }
  .runtime-feature p { margin: 0; color: var(--muted); font-size: 13px; line-height: 1.72; }
  .runtime-feature code { color: #d2b48f; }
  .runtime-callout { display: grid; grid-template-columns: 34px 1fr; gap: 15px; margin-top: 15px; padding: 20px; border: 1px solid rgba(255,122,24,.18); border-radius: 7px; background: rgba(255,122,24,.03); }
  .callout-mark { display: grid; place-items: center; width: 29px; height: 29px; border: 1px solid rgba(255,122,24,.25); border-radius: 6px; color: var(--accent); font: 13px "SFMono-Regular", Consolas, monospace; }
  .runtime-callout strong { display: block; margin-bottom: 4px; color: #d5dce1; font-size: 12px; }
  .runtime-callout span:last-child { display: block; color: #75818b; font-size: 11px; line-height: 1.6; }

  .flow-grid { display: grid; grid-template-columns: 1fr auto 1fr auto 1fr; align-items: stretch; gap: 10px; }
  .flow-card { min-height: 245px; padding: 25px; border: 1px solid var(--line); border-radius: 8px; background: rgba(255,255,255,.015); }
  .flow-card.active { border-color: rgba(255,122,24,.25); background: linear-gradient(145deg, rgba(255,122,24,.05), rgba(255,255,255,.015)); }
  .flow-index { color: var(--accent); font: 9px "SFMono-Regular", Consolas, monospace; }
  .flow-card h3 { margin: 43px 0 9px; font-size: 19px; }
  .flow-card p { margin: 0; color: var(--muted); font-size: 12px; line-height: 1.72; }
  .flow-code { margin-top: 36px; color: #59656f; font-size: 8px; letter-spacing: .06em; }
  .flow-arrow { display: grid; place-items: center; color: #46515b; font: 18px "SFMono-Regular", Consolas, monospace; }
  .decision-band { display: flex; align-items: center; justify-content: center; gap: 32px; margin-top: 35px; padding: 22px; border: 1px solid var(--line); border-radius: 8px; background: rgba(255,255,255,.012); }
  .decision-group { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
  .decision-title { margin-right: 3px; color: #53606b; font: 8px "SFMono-Regular", Consolas, monospace; letter-spacing: .1em; }
  .decision-pill { padding: 5px 7px; border-radius: 4px; font: 8px "SFMono-Regular", Consolas, monospace; letter-spacing: .06em; }
  .decision-pill.pass, .decision-pill.allow { color: #a8d8bf; border: 1px solid rgba(100,215,155,.16); background: rgba(100,215,155,.06); }
  .decision-pill.review { color: #e7c183; border: 1px solid rgba(232,193,110,.16); background: rgba(232,193,110,.06); }
  .decision-pill.fail, .decision-pill.deny { color: #ef9b94; border: 1px solid rgba(255,108,103,.16); background: rgba(255,108,103,.06); }
  .decision-divider { width: 1px; height: 22px; background: var(--line-strong); }

  .verifier-table { display: grid; grid-template-columns: repeat(4, 1fr); gap: 1px; background: #cfd3d0; border: 1px solid #cfd3d0; }
  .verifier-table article { min-height: 330px; padding: 27px; background: var(--paper-2); }
  .verifier-head { display: flex; justify-content: space-between; color: #8a9398; font-size: 8px; letter-spacing: .1em; }
  .verifier-head b { color: #30373c; font-weight: 700; }
  .verifier-table h3 { margin: 50px 0 9px; color: #171d21; font-size: 16px; line-height: 1.25; }
  .verifier-table p { min-height: 99px; margin: 0; color: var(--ink-muted); font-size: 12px; line-height: 1.72; }
  .guarantee { color: #5b676d; font-size: 8px; letter-spacing: .1em; }
  .guarantee::before { content: "●"; margin-right: 6px; color: #4ca273; }
  .verifier-note { display: grid; grid-template-columns: 34px 1fr; gap: 14px; margin-top: 15px; padding: 20px; border: 1px solid #cfd3d0; background: rgba(255,255,255,.52); }
  .note-symbol { display: grid; place-items: center; width: 29px; height: 29px; color: #677279; border: 1px solid #c5cbc8; border-radius: 6px; font: 15px "SFMono-Regular", Consolas, monospace; }
  .verifier-note strong { display: block; margin-bottom: 3px; font-size: 12px; color: #252c31; }
  .verifier-note p { margin: 0; color: #667178; font-size: 11px; line-height: 1.65; }

  .two-col { display: grid; grid-template-columns: .88fr 1.12fr; gap: 100px; align-items: start; }
  .policy-section { padding-bottom: 140px; }
  .text-link { display: inline-flex; gap: 7px; margin-top: 24px; color: #c7cfd5; font: 9px "SFMono-Regular", Consolas, monospace; text-transform: uppercase; letter-spacing: .09em; }
  .text-link span { color: var(--accent); }
  .code-card { overflow: hidden; border: 1px solid #2a333d; border-radius: 8px; background: #0b0f13; }
  .code-card-top, .code-card-foot { padding: 13px 16px; color: #58636e; border-bottom: 1px solid var(--line); font: 8px "SFMono-Regular", Consolas, monospace; letter-spacing: .08em; }
  .code-card-top span:last-child { color: var(--green); }
  .code-card pre { margin: 0; padding: 28px; overflow-x: auto; font-size: 11px; line-height: 1.85; }
  .code-card-foot { border-top: 1px solid var(--line); border-bottom: 0; }
  .yaml-key { color: #d2d9de; }
  .yaml-string { color: #94c5a5; }
  .yaml-deny { color: #ef9993; }
  .yaml-review { color: #e5bf78; }
  .yaml-allow { color: #94c5a5; }

  .evidence-section { padding-top: 135px; padding-bottom: 140px; }
  .evidence-layout { align-items: center; }
  .evidence-points { margin-top: 36px; border-top: 1px solid var(--line); }
  .evidence-points > div { display: grid; grid-template-columns: 31px 1fr; gap: 0 12px; padding: 16px 0; border-bottom: 1px solid var(--line); }
  .evidence-points span { color: var(--accent); padding-top: 2px; font: 8px "SFMono-Regular", Consolas, monospace; }
  .evidence-points strong { font-size: 13px; font-weight: 600; }
  .evidence-points p { grid-column: 2; margin: 3px 0 0; color: #67737d; font-size: 11px; line-height: 1.55; }
  .record-card { overflow: hidden; border: 1px solid #28323c; border-radius: 9px; background: #0a0e12; box-shadow: 0 35px 100px rgba(0,0,0,.28); }
  .record-card-top, .record-card-foot { padding: 13px 16px; color: #596570; font: 8px "SFMono-Regular", Consolas, monospace; letter-spacing: .08em; }
  .record-card-top { border-bottom: 1px solid var(--line); }
  .record-card-top span:last-child { color: var(--green); }
  .record-card pre { margin: 0; padding: 27px 29px; overflow-x: auto; font: 10px/1.9 "SFMono-Regular", Consolas, monospace; }
  .record-card-foot { justify-content: flex-start; gap: 25px; border-top: 1px solid var(--line); }
  .record-card-foot span { color: #66727c; }
  .green-dot { display: inline-block; width: 5px; height: 5px; margin-right: 5px; border-radius: 50%; background: var(--green); }
  .j-brace, .j-bracket { color: #7b8791; }
  .j-key { color: #d3dae0; }
  .j-string { color: #94c6a7; }
  .j-review { color: #e6c17d; }

  .environment-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 1px; border: 1px solid var(--line); background: var(--line); }
  .environment-grid article { min-height: 265px; padding: 28px; background: #0b0f13; }
  .env-icon { display: grid; place-items: center; width: 29px; height: 29px; border: 1px solid #2a343d; border-radius: 5px; color: var(--accent); font-size: 8px; }
  .environment-grid h3 { margin: 40px 0 8px; font-size: 16px; }
  .environment-grid p { margin: 0; color: var(--muted); font-size: 12px; line-height: 1.72; }
  .environment-grid code { color: #d2b48f; }

  .stack-section { padding-top: 120px; padding-bottom: 125px; }
  .stack-diagram { max-width: 1010px; margin: 0 auto; }
  .stack-row { display: flex; flex-wrap: wrap; justify-content: center; gap: 8px; }
  .stack-row span { padding: 10px 12px; border: 1px solid var(--line); border-radius: 6px; background: rgba(255,255,255,.018); color: #bac3ca; font: 9px "SFMono-Regular", Consolas, monospace; }
  .stack-arrow { padding: 22px 0; text-align: center; color: #4a5560; font: 9px "SFMono-Regular", Consolas, monospace; letter-spacing: .08em; text-transform: uppercase; }
  .stack-core { padding: 1px; border-radius: 10px; background: linear-gradient(135deg, rgba(255,122,24,.48), rgba(255,122,24,.04) 52%, rgba(100,215,155,.14)); }
  .stack-core-label { padding: 13px 17px; border-radius: 9px 9px 0 0; background: #11161b; color: #d0d6db; font-size: 8px; letter-spacing: .12em; }
  .stack-core-grid { display: grid; grid-template-columns: repeat(4, 1fr); background: #0c1015; border-radius: 0 0 9px 9px; }
  .stack-core-grid > div { min-height: 110px; padding: 25px; border-right: 1px solid var(--line); }
  .stack-core-grid > div:last-child { border-right: 0; }
  .stack-core-grid strong { display: block; margin-bottom: 5px; font-size: 13px; }
  .stack-core-grid small { color: #56626d; font: 8px "SFMono-Regular", Consolas, monospace; }
  .bottom-stack span { border-color: rgba(100,215,155,.12); color: #a7cdb8; }

  .usecase-section { padding-bottom: 145px; }
  .usecase-header { align-items: end; margin-bottom: 55px; }
  .usecase-header .section-intro { margin-bottom: 0; }
  .usecase-lead { margin: 0; color: #71808b; font-size: 13px; line-height: 1.7; }
  .usecase-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 1px; border: 1px solid var(--line); background: var(--line); }
  .usecase-grid article { min-height: 240px; padding: 28px; background: #0b0f13; }
  .usecase-tag { color: var(--accent); font: 8px "SFMono-Regular", Consolas, monospace; letter-spacing: .12em; }
  .usecase-grid h3 { margin: 42px 0 9px; font-size: 18px; }
  .usecase-grid p { margin: 0; max-width: 50ch; color: var(--muted); font-size: 12px; line-height: 1.7; }
  .usecase-line { margin-top: 38px; color: #4c5964; font-size: 8px; }

  .technical-section { padding-top: 125px; padding-bottom: 130px; }
  .technical-list { border-top: 1px solid #cbd0cd; }
  .technical-list > div { display: grid; grid-template-columns: 62px 1fr; gap: 0 17px; padding: 18px 0; border-bottom: 1px solid #cbd0cd; }
  .technical-list span { color: #8a9498; font: 8px "SFMono-Regular", Consolas, monospace; letter-spacing: .08em; }
  .technical-list strong { color: #20282e; font-size: 13px; font-weight: 650; }
  .technical-list p { grid-column: 2; margin: 4px 0 0; color: #68747a; font-size: 11px; line-height: 1.6; }

  .quickstart-section { padding-top: 125px; padding-bottom: 125px; }
  .quick-links { display: flex; flex-wrap: wrap; gap: 12px 24px; }
  .quickstart-code { overflow: hidden; border: 1px solid #2a333c; border-radius: 8px; background: #0a0e12; }
  .quickstart-tab { padding: 12px 16px; border-bottom: 1px solid var(--line); color: #596570; font: 8px "SFMono-Regular", Consolas, monospace; letter-spacing: .08em; }
  .quickstart-code pre { margin: 0; padding: 25px 26px; overflow-x: auto; font: 10px/1.85 "SFMono-Regular", Consolas, monospace; color: #c5cdd3; }
  .quickstart-code .prompt { color: var(--accent); }
  .quickstart-code .comment { color: #56626d; }
  .quickstart-code .string { color: #94c6a7; }
  .quickstart-foot { padding: 12px 16px; border-top: 1px solid var(--line); color: #515d68; font: 8px "SFMono-Regular", Consolas, monospace; }

  .final-section { min-height: 690px; display: flex; align-items: center; background: radial-gradient(580px 360px at 50% 38%, rgba(255,122,24,.11), transparent 72%), #090c10; }
  .final-cta { max-width: 890px; margin: 0 auto; text-align: center; }
  .final-mark { display: flex; align-items: end; justify-content: center; gap: 4px; height: 28px; margin-bottom: 35px; transform: skewX(-18deg); }
  .final-mark i { display: block; width: 4px; border-radius: 2px; background: var(--accent); }
  .final-mark i:nth-child(1) { height: 13px; opacity: .42; }
  .final-mark i:nth-child(2) { height: 22px; opacity: .7; }
  .final-mark i:nth-child(3) { height: 29px; }
  .final-cta h2 { margin-top: 13px; }
  .final-cta p { max-width: 72ch; margin: 0 auto; color: #7d8993; font-size: 14px; line-height: 1.8; }
  .center-actions { justify-content: center; }
  .final-meta { display: flex; justify-content: center; gap: 10px; margin-top: 26px; color: #515d68; font-size: 8px; letter-spacing: .08em; }
  .final-meta a:hover { color: #8b97a1; }

  .footer { min-height: 86px; display: grid; grid-template-columns: auto 1fr auto auto; gap: 26px; align-items: center; color: #525e68; font-size: 10px; }
  .footer-brand { color: #c7ced4; font-weight: 650; }
  .footer-copy { padding-left: 25px; border-left: 1px solid var(--line); }
  .footer a { color: #8b97a0; }
  .copyright { color: #46515b; }

  @media (max-width: 1100px) {
    .hero { grid-template-columns: 1fr; gap: 58px; }
    .hero-copy { max-width: 860px; }
    .hero-panel { max-width: 760px; }
    .execution-layout, .two-col { grid-template-columns: 1fr; gap: 55px; }
    .verifier-table { grid-template-columns: repeat(2, 1fr); }
    .environment-grid { grid-template-columns: repeat(2, 1fr); }
    .usecase-header { align-items: start; }
    .quickstart-code { max-width: 760px; }
  }

  @media (max-width: 820px) {
    .shell { width: min(calc(100% - 40px), var(--max)); }
    .nav { grid-template-columns: auto 1fr auto; min-height: 70px; }
    .nav-links { display: none; }
    .nav-cta { margin-left: auto; }
    .statement-grid, .problem-grid, .flow-grid, .usecase-grid { grid-template-columns: 1fr; }
    .statement-grid > div, .statement-grid > div:first-child, .statement-grid > div:last-child { min-height: auto; padding: 22px 0; border-right: 0; border-bottom: 1px solid var(--line); }
    .statement-grid > div:last-child { border-bottom: 0; }
    .problem-card { min-height: 0; }
    .problem-card h3 { margin-top: 38px; }
    .card-caption { margin-top: 30px; }
    .flow-arrow { min-height: 20px; transform: rotate(90deg); }
    .decision-band { flex-direction: column; gap: 18px; }
    .decision-divider { width: 100%; height: 1px; }
    .stack-core-grid { grid-template-columns: 1fr 1fr; }
    .stack-core-grid > div { border-bottom: 1px solid var(--line); }
    .stack-core-grid > div:nth-child(2) { border-right: 0; }
    .stack-core-grid > div:nth-last-child(-n+2) { border-bottom: 0; }
    .footer { grid-template-columns: 1fr 1fr; gap: 13px; padding-top: 24px; padding-bottom: 24px; }
    .footer-copy { display: none; }
  }

  @media (max-width: 560px) {
    .shell { width: calc(100% - 30px); }
    .nav-cta { padding: 9px 10px; }
    .hero { min-height: auto; padding-top: 56px; padding-bottom: 64px; }
    h1 { font-size: clamp(3rem, 14vw, 4.5rem); }
    h2 { font-size: 2.65rem; }
    .hero-lead { font-size: 18px; }
    .hero-actions { flex-direction: column; align-items: stretch; }
    .button { width: 100%; }
    .trace-footer { align-items: flex-start; flex-direction: column; gap: 10px; }
    .hero-panel-caption { flex-wrap: wrap; }
    .section { padding-top: 88px; padding-bottom: 88px; }
    .section-intro { margin-bottom: 42px; }
    .verifier-table, .environment-grid { grid-template-columns: 1fr; }
    .verifier-table article { min-height: 0; }
    .verifier-table h3 { margin-top: 38px; }
    .verifier-table p { min-height: 0; margin-bottom: 24px; }
    .execution-rail::before { left: 18px; }
    .runtime-feature { grid-template-columns: 30px 1fr; gap: 10px; }
    .record-card pre, .code-card pre, .quickstart-code pre { padding-left: 16px; padding-right: 16px; font-size: 9px; }
    .technical-list > div { grid-template-columns: 55px 1fr; }
    .final-section { min-height: 610px; }
    .final-meta { flex-wrap: wrap; }
  }

  @media (prefers-reduced-motion: reduce) {
    html { scroll-behavior: auto; }
    *, *::before, *::after { transition: none !important; transform: none !important; }
  }

  /* Light visual system based on the GroundLens mark. */
  :root {
    --bg: #ffffff;
    --panel: #f7f9fb;
    --panel-2: #f1f5f8;
    --panel-3: #e8eef3;
    --text: #142536;
    --muted: #455766;
    --muted-2: #61717e;
    --muted-3: #71808b;
    --line: rgba(20, 37, 54, .14);
    --line-strong: rgba(20, 37, 54, .26);
    --accent: #e65348;
    --accent-soft: rgba(230, 83, 72, .12);
    --blue: #155189;
    --green: #287a5b;
    --red: #bd3936;
    --paper: #eef3f6;
    --paper-2: #ffffff;
    --ink: #142536;
    --ink-muted: #526473;
  }

  body {
    background: var(--bg);
    color: var(--text);
    font-family: "Geist", ui-sans-serif, system-ui, sans-serif;
  }

  h1, h2, h3, h4, h5, h6 {
    font-family: "Geist", ui-sans-serif, system-ui, sans-serif;
    letter-spacing: .01em;
  }

  code, pre, .mono-label, .kicker, .micro, .guarantee, .window-title, .window-state,
  .card-caption, .flow-code, .usecase-line, .quickstart-tab, .final-meta, .env-icon,
  .verifier-head, .stack-core-label, .yaml-key, .yaml-string, .yaml-deny, .yaml-review,
  .yaml-allow, .j-key, .j-string, .j-review, .j-brace, .j-bracket {
    font-family: "Geist Mono", ui-monospace, monospace;
  }

  .nav { border-bottom-color: var(--line); }
  .brand { color: var(--blue); font-size: 1.12rem; }
  .nav-links { gap: 32px; }
  .nav-links a { color: var(--blue); font-size: 15px; }
  .nav-links a span, .nav-cta span { color: var(--accent); }
  .nav-cta { border-color: var(--line-strong); background: var(--panel); color: var(--blue); font-size: 14px; }
  .nav-cta:hover { border-color: var(--accent); background: var(--accent-soft); }

  .eyebrow, .kicker, .micro { color: var(--blue); font-size: 12px; }
  h1 { color: var(--blue); letter-spacing: .015em; font-weight: 650; }
  h1 span, h2 span { color: var(--accent); }
  h2 { letter-spacing: .01em; }
  .hero-lead { color: #263f52; font-size: 22px; }
  .hero-lead strong { color: var(--blue); }
  .hero-body, .section-intro p { color: var(--muted); font-size: 17px; }
  .hero-facts { color: var(--blue); font-size: 13px; }
  .hero-facts span + span::before { color: var(--accent); }

  .hero-panel-caption { color: var(--blue); font-size: 11px; }
  .hero-panel-caption i { background: var(--accent); }
  .statement-strip, .section-dark { background: var(--panel); border-color: var(--line); }
  .statement-grid strong { color: var(--text); font-size: 16px; }
  .section-light { background: var(--paper); }
  .problem-grid, .environment-grid { background: var(--line-strong); }
  .problem-card, .environment-grid article, .usecase-grid article { background: var(--panel-2); }
  .problem-card.emphasis-card { background: #fff2ef; }
  .problem-card h3, .runtime-feature h3, .flow-card h3, .environment-grid h3, .usecase-grid h3 { color: var(--blue); }
  .problem-card p, .runtime-feature p, .flow-card p, .environment-grid p, .usecase-grid p { color: var(--muted); font-size: 16px; }
  .card-top, .card-caption, .flow-code, .usecase-line { color: var(--blue); font-size: 12px; }
  .runtime-feature { border-color: var(--line); }
  .execution-rail::before { background: var(--line); }
  .rail-item > span { background: var(--panel); border-color: var(--line-strong); color: var(--blue); }
  .rail-item strong { color: var(--blue); font-size: 17px; }
  .rail-item small { color: var(--muted-2); font-size: 12px; }
  .flow-card, .decision-band, .runtime-callout { border-color: var(--line); background: var(--panel); }
  .flow-card.active { border-color: var(--accent); background: #fff2ef; }
  .flow-index, .flow-arrow, .decision-title { color: var(--accent); }
  .verifier-table { background: var(--line-strong); border-color: var(--line-strong); }
  .verifier-table article { background: var(--paper-2); }
  .verifier-table h3, .technical-list strong { color: var(--blue); }
  .verifier-table p, .technical-list p { color: var(--muted); font-size: 15px; }
  .code-card, .record-card, .quickstart-code, .trace-window { background: var(--panel-2); border-color: var(--line-strong); }
  .code-card pre, .record-card pre, .quickstart-code pre { color: var(--text); font-size: 13px; }
  .stack-row span, .stack-core-grid { background: var(--panel); }
  .stack-row span { color: var(--blue); }
  .stack-core { background: linear-gradient(135deg, rgba(230,83,72,.6), rgba(21,81,137,.35)); }
  .stack-core-label { background: var(--blue); color: white; font-size: 11px; }
  .final-section { background: var(--paper); }
  .final-cta p { color: var(--muted); font-size: 17px; }
  .footer { color: var(--muted-2); font-size: 13px; }
  .footer { grid-template-columns: 1fr auto 1fr; }
  .footer a { color: var(--blue); }
  .footer a:last-child { text-align: right; }
  .copyright { color: var(--blue); text-align: center; }

  .window-bar, .trace-footer { border-color: var(--line); background: var(--panel-2); }
  .window-title, .window-state, .trace-line, .trace-footer strong, .verified { color: var(--blue); }
  .trace-line { border-color: var(--line); font-size: 13px; }
  .trace-line b, .trace-line small, .mono-label { font-size: 11px; }
  .trace-line em { color: var(--text); }
  .verified { color: var(--green); }

  .kicker, .eyebrow, .micro, .card-top, .card-caption, .flow-code,
  .usecase-line, .usecase-tag, .guarantee, .verifier-head, .stack-core-label,
  .stack-row span, .stack-arrow, .decision-title, .decision-pill, .text-link, .env-icon,
  .quickstart-tab, .quickstart-foot, .final-meta, .footer, .window-title,
  .window-state, .mono-label, .trace-line b, .trace-line small, .feature-number,
  .rail-item small, .evidence-points span, .technical-list span, .record-card-top,
  .record-card-foot, .code-card-top, .code-card-foot { font-size: 12px; }

  .evidence-points p { font-size: 12px; }

  .decision-band { gap: 42px; }
  .decision-title { display: flex; flex-direction: column; gap: 4px; color: var(--blue); font-weight: 700; }
  .decision-title small { color: var(--muted); font-size: 12px; font-weight: 400; letter-spacing: 0; text-transform: none; }
  .decision-pill { padding: 7px 9px; font-weight: 700; }

  .runtime-callout strong, .runtime-callout span:last-child { color: #142536; }
  .runtime-callout span:last-child { font-size: 14px; }
  .verifier-note strong { color: #142536; font-size: 14px; }
  .verifier-note p { color: #142536; font-size: 14px; }

  .code-card, .record-card { background: #142536; border-color: #0b1a28; }
  .code-card-top, .code-card-foot, .record-card-top, .record-card-foot { color: #dbe8f2; }
  .code-card pre, .record-card pre { color: #f5f8fa; }
  .yaml-key, .j-key { color: #f5c6a9; }
  .yaml-string, .j-string { color: #b9e5c7; }
  .yaml-deny, .j-review { color: #ff9c91; }
  .yaml-review { color: #ffd58b; }
  .yaml-allow { color: #8ee0bd; }
  .code-card-top span:last-child, .record-card-top span:last-child { color: #8ee0bd; }

  .stack-row span, .stack-core-grid strong, .stack-core-grid small { font-size: 13px; }
  .stack-core-grid small { color: #d5e1e9; }

  .button, .nav-cta, .nav-links a { font-size: 14px; }
  .final-meta { letter-spacing: .04em; }

  @media (max-width: 820px) {
    .nav-links a { font-size: 16px; }
    .nav-cta { font-size: 15px; }
  }

  @media (max-width: 560px) {
    h1 { font-size: clamp(2.75rem, 14vw, 4.5rem); letter-spacing: .01em; }
    h2 { font-size: 2.65rem; }
    .hero-lead { font-size: 19px; }
    .hero-body, .section-intro p { font-size: 16px; }
    .nav-cta { font-size: 14px; }
  }

  .trace-window {
    background: #f5f5f7;
    border-color: #c7c7cc;
    box-shadow: 0 10px 24px rgba(230, 83, 72, .14);
  }

  .window-bar {
    background: #e5e5e7;
    border-bottom-color: #c7c7cc;
  }

  .window-dots i:nth-child(1) { background: #ff5f57; }
  .window-dots i:nth-child(2) { background: #febc2e; }
  .window-dots i:nth-child(3) { background: #28c840; }

  .window-title, .window-state { color: #59636d; }
  .trace-body { background: #f5f5f7; }
  .trace-line { color: #142536; border-bottom-color: #d7d7dc; }
  .trace-line > span { color: #68737d; }
  .trace-line b { color: #304252; font-weight: 700; }
  .trace-line em { color: #142536; }
  .trace-line small { color: #59636d; }
  .trace-line:nth-child(3) small,
  .trace-line:nth-child(4) small,
  .trace-line .allow,
  .trace-line .review { font-weight: 800; }
  .trace-line .allow { color: #19734d !important; }
  .trace-line .review { color: #a45b00 !important; }
  .trace-line.accent { border-color: rgba(230, 83, 72, .3); background: rgba(230, 83, 72, .08); }
  .trace-footer { background: #e5e5e7; border-top-color: #c7c7cc; }
  .trace-footer strong, .mono-label { color: #142536; }
  .verified { color: #19734d; font-weight: 700; }

  /* Consolidated editorial layout: one blue accent, white surfaces, quiet contrast. */
  :root {
    --bg: #ffffff;
    --panel: #ffffff;
    --panel-2: #f7faff;
    --panel-3: #edf4fb;
    --text: #142536;
    --muted: #536575;
    --muted-2: #6b7c8c;
    --muted-3: #718395;
    --line: #dce6ef;
    --line-strong: #c5d5e3;
    --accent: #155189;
    --accent-soft: rgba(21, 81, 137, .09);
    --blue: #155189;
    --green: #155189;
    --red: #155189;
    --paper: #f5f8fb;
    --paper-2: #ffffff;
    --ink: #142536;
    --ink-muted: #536575;
  }

  html { background: #ffffff; }
  body {
    background: #ffffff;
    color: var(--text);
    font-family: "Geist", ui-sans-serif, system-ui, sans-serif;
    font-size: 16px;
  }

  .site-shell { background: #ffffff; }
  .noise { display: none; }
  .shell { width: min(calc(100% - 64px), 1180px); }
  a { color: var(--blue); }

  .nav {
    min-height: 76px;
    border-bottom: 1px solid var(--line);
  }
  .brand { color: var(--text); font-size: 1.1rem; letter-spacing: 0; }
  .nav-links { gap: 24px; }
  .nav-links a { color: var(--text); font-size: 14px; }
  .nav-links a:hover, .footer a:hover, .text-link:hover { color: var(--blue); }
  .nav-links a span, .nav-cta span { color: var(--blue); }
  .nav-cta {
    border-color: var(--blue);
    border-radius: 5px;
    background: var(--blue);
    color: #ffffff;
    font-size: 14px;
  }
  .nav-cta:hover { border-color: #0f3c68; background: #0f3c68; }

  .hero {
    min-height: 670px;
    gap: 72px;
    padding-top: 88px;
    padding-bottom: 88px;
  }
  .hero-copy { max-width: 650px; }
  .eyebrow, .kicker, .micro { color: var(--blue); font-size: 12px; letter-spacing: .11em; }
  .pulse { background: var(--blue); box-shadow: 0 0 0 4px rgba(21, 81, 137, .1); }
  h1, h2, h3, h4, h5, h6 { color: var(--text); }
  h1 {
    margin: 20px 0 28px;
    color: var(--text);
    font-size: clamp(3.4rem, 6.2vw, 6.2rem);
    line-height: .98;
    letter-spacing: -.025em;
    font-weight: 650;
  }
  h1 span, h2 span { color: var(--blue); }
  h2 {
    color: var(--text);
    font-size: clamp(2.4rem, 4vw, 4.3rem);
    line-height: 1.04;
    letter-spacing: -.02em;
    font-weight: 620;
  }
  .hero-lead { color: #2c455a; font-size: 21px; line-height: 1.55; }
  .hero-lead strong { color: var(--text); font-weight: 650; }
  .hero-body, .section-intro p { color: var(--muted); font-size: 17px; line-height: 1.7; }
  .hero-actions { margin-top: 32px; }
  .button { border-radius: 5px; font-size: 14px; min-height: 46px; }
  .button-primary { background: var(--blue); border-color: var(--blue); color: #ffffff; }
  .button-primary:hover { background: #0f3c68; }
  .button-secondary { border-color: var(--line-strong); background: #ffffff; color: var(--blue); }
  .button-secondary:hover { border-color: var(--blue); background: var(--panel-2); }
  .hero-facts { color: var(--muted-2); font-size: 12px; }
  .hero-facts span + span::before { color: var(--line-strong); }

  .statement-strip {
    background: var(--panel-2);
    border-top: 1px solid var(--line);
    border-bottom: 1px solid var(--line);
  }
  .statement-grid strong { color: var(--text); font-size: 16px; line-height: 1.5; }
  .section, .section-dark, .section-light { background: #ffffff; color: var(--text); }
  .section-dark, .section-light { border-color: var(--line); }
  .section-dark { background: var(--panel-2); }
  .section-intro { margin-bottom: 52px; }
  .section-intro p { max-width: 68ch; }
  .kicker { font-weight: 650; }

  .problem-grid, .environment-grid, .usecase-grid, .verifier-table {
    gap: 1px;
    border: 1px solid var(--line);
    background: var(--line);
  }
  .problem-card, .environment-grid article, .usecase-grid article, .verifier-table article {
    background: #ffffff;
  }
  .problem-card.emphasis-card { background: var(--panel-2); }
  .problem-card h3, .runtime-feature h3, .flow-card h3, .environment-grid h3,
  .usecase-grid h3, .verifier-table h3, .technical-list strong { color: var(--text); }
  .problem-card p, .runtime-feature p, .flow-card p, .environment-grid p,
  .usecase-grid p, .verifier-table p, .technical-list p { color: var(--muted); }
  .problem-card p, .runtime-feature p, .flow-card p, .environment-grid p,
  .usecase-grid p { font-size: 16px; }
  .card-top, .card-caption, .flow-code, .usecase-line { color: var(--blue); }
  .emphasis-card .card-caption { color: var(--blue); }

  .execution-layout { gap: 80px; }
  .execution-rail::before { background: var(--line-strong); }
  .rail-item > span { background: #ffffff; border-color: var(--line-strong); color: var(--blue); }
  .rail-item strong { color: var(--text); }
  .rail-item small { color: var(--muted-2); }
  .runtime-feature { border-color: var(--line); }
  .runtime-callout { border-color: var(--line-strong); background: var(--panel-2); }
  .runtime-callout strong, .runtime-callout span:last-child { color: var(--text); }
  .callout-mark { border-color: var(--blue); color: var(--blue); }

  .flow-card, .decision-band { border-color: var(--line); background: #ffffff; }
  .flow-card.active { border-color: var(--blue); background: var(--panel-2); }
  .flow-index, .flow-arrow, .decision-title { color: var(--blue); }
  .decision-pill { border-color: var(--line-strong) !important; background: var(--panel-2) !important; color: var(--blue) !important; }

  .section-light { background: var(--panel-2); }
  .verifier-table { background: var(--line); }
  .verifier-table article { background: #ffffff; }
  .verifier-head { color: var(--muted-2); }
  .verifier-head b { color: var(--text); }
  .guarantee { color: var(--blue); }
  .guarantee::before { color: var(--blue); }
  .verifier-note { border-color: var(--line); background: #ffffff; }
  .verifier-note strong, .verifier-note p { color: var(--text); }

  .code-card, .record-card, .quickstart-code, .trace-window {
    background: #ffffff;
    border: 1px solid var(--line-strong);
    box-shadow: 0 16px 36px rgba(21, 81, 137, .08);
  }
  .code-card-top, .code-card-foot, .record-card-top, .record-card-foot,
  .quickstart-tab, .quickstart-foot { color: var(--muted); border-color: var(--line); }
  .code-card pre, .record-card pre, .quickstart-code pre { color: var(--text); }
  .yaml-key, .j-key { color: var(--blue); }
  .yaml-string, .j-string { color: #356c9f; }
  .yaml-deny, .yaml-review, .yaml-allow, .j-review { color: var(--blue); }

  .evidence-points { border-color: var(--line); }
  .evidence-points > div { border-color: var(--line); }
  .evidence-points span { color: var(--blue); }
  .evidence-points strong { color: var(--text); }
  .evidence-points p { color: var(--muted); }

  .stack-section { background: var(--panel-2); }
  .stack-row span { background: #ffffff; border-color: var(--line-strong); color: var(--blue); }
  .stack-arrow { color: var(--muted-2); }
  .stack-core { background: var(--blue); }
  .stack-core-label { background: var(--blue); color: #ffffff; }
  .stack-core-grid { background: #ffffff; }
  .stack-core-grid > div { border-color: var(--line); }
  .stack-core-grid strong { color: var(--text); }
  .stack-core-grid small { color: var(--muted); }
  .bottom-stack span { border-color: var(--line-strong); color: var(--blue); }

  .usecase-lead { color: var(--muted); font-size: 16px; }
  .usecase-tag { color: var(--blue); }
  .technical-list { border-color: var(--line); }
  .technical-list > div { border-color: var(--line); }
  .technical-list span { color: var(--blue); }

  .final-section { min-height: 620px; background: var(--panel-2); }
  .final-cta p { color: var(--muted); }
  .final-mark i { background: var(--blue); }
  .final-meta { color: var(--muted); }
  .final-meta a { color: var(--blue); }
  .footer { min-height: 76px; color: var(--muted); border-top: 1px solid var(--line); }
  .footer a, .copyright { color: var(--blue); }

  .trace-window { box-shadow: 0 16px 36px rgba(21, 81, 137, .1); }
  .window-bar, .trace-footer { background: var(--panel-2); border-color: var(--line); }
  .window-dots i { background: var(--blue) !important; opacity: .65; }
  .window-dots i:nth-child(2) { opacity: .45; }
  .window-dots i:nth-child(3) { opacity: .28; }
  .trace-body { background: #ffffff; }
  .trace-line { color: var(--text); border-color: var(--line); }
  .trace-line > span, .trace-line b, .trace-line small { color: var(--muted-2); }
  .trace-line em { color: var(--text); }
  .trace-line .allow, .trace-line .review { color: var(--blue) !important; }
  .trace-line.accent { border-color: var(--line-strong); background: var(--panel-2); }
  .trace-footer strong, .mono-label, .verified { color: var(--blue); }

  @media (max-width: 820px) {
    .shell { width: min(calc(100% - 40px), 1180px); }
    .nav-cta { background: var(--blue); color: #ffffff; }
    .footer { grid-template-columns: 1fr 1fr; }
  }

  @media (max-width: 560px) {
    .shell { width: calc(100% - 30px); }
    h1 { font-size: clamp(2.8rem, 13vw, 4.5rem); }
    h2 { font-size: 2.7rem; }
    .hero-lead { font-size: 19px; }
  }

  /* Component redesign inspired by Stripe's editorial hierarchy. */
  body {
    font-size: 17px;
    line-height: 1.55;
    letter-spacing: 0;
  }

  .shell { width: min(calc(100% - 80px), 1240px); }
  .nav {
    min-height: 84px;
    grid-template-columns: auto 1fr auto;
    gap: 40px;
    border-bottom: 0;
  }
  .brand { font-size: 1.2rem; font-weight: 700; }
  .brand-mark { width: 34px; height: 34px; }
  .brand-mark img { width: 32px; height: 32px; }
  .nav-links { gap: 30px; }
  .nav-links a { font-size: 15px; font-weight: 560; }
  .nav-cta {
    min-height: 42px;
    padding: 0 18px;
    border-radius: 999px;
    font-size: 15px;
    font-weight: 650;
  }

  .hero {
    min-height: 760px;
    grid-template-columns: minmax(0, 1.05fr) minmax(420px, .95fr);
    gap: 96px;
    padding-top: 112px;
    padding-bottom: 128px;
  }
  .hero-copy { max-width: 720px; }
  .eyebrow { font-size: 13px; font-weight: 650; letter-spacing: .08em; }
  h1 {
    margin: 22px 0 30px;
    max-width: 760px;
    font-size: clamp(4.2rem, 7.2vw, 7.4rem);
    line-height: .96;
    letter-spacing: -.055em;
    font-weight: 620;
  }
  h2 {
    margin: 18px 0 24px;
    font-size: clamp(3rem, 5vw, 5.4rem);
    line-height: .99;
    letter-spacing: -.045em;
    font-weight: 620;
  }
  h3 { letter-spacing: -.018em; font-weight: 650; }
  .hero-lead { max-width: 680px; font-size: 23px; line-height: 1.45; }
  .hero-body { max-width: 650px; font-size: 17px; line-height: 1.7; }
  .hero-actions { gap: 14px; margin-top: 36px; }
  .button {
    min-height: 52px;
    padding: 0 22px;
    border-radius: 999px;
    font-size: 16px;
    font-weight: 650;
  }
  .hero-facts { gap: 10px 18px; margin-top: 30px; font-size: 13px; }

  .trace-window {
    border-radius: 16px;
    box-shadow: 0 24px 60px rgba(21, 81, 137, .12);
  }
  .window-bar { min-height: 48px; padding: 0 18px; }
  .window-dots { gap: 6px; }
  .window-dots i { width: 8px; height: 8px; }
  .window-title, .window-state { font-size: 12px; letter-spacing: .04em; }
  .trace-body { padding: 18px 20px 14px; }
  .trace-line {
    grid-template-columns: 32px 92px 1fr auto;
    gap: 12px;
    min-height: 48px;
    font-size: 13px;
  }
  .trace-line b { font-size: 12px; }
  .trace-line small { font-size: 12px; }
  .trace-footer { padding: 18px 20px; }
  .trace-footer strong { font-size: 14px; }
  .mono-label, .verified { font-size: 12px; }
  .hero-panel-caption { margin-top: 20px; font-size: 12px; }

  .statement-grid { grid-template-columns: repeat(3, 1fr); }
  .statement-grid > div { min-height: 176px; padding: 38px 34px; }
  .statement-grid strong { font-size: 17px; line-height: 1.45; }

  .section { padding-top: 156px; padding-bottom: 156px; }
  .section-intro { max-width: 820px; margin-bottom: 68px; }
  .section-intro p { max-width: 66ch; font-size: 18px; line-height: 1.7; }
  .kicker { font-size: 13px; letter-spacing: .08em; }

  .problem-grid, .environment-grid, .usecase-grid, .verifier-table {
    gap: 16px;
    border: 0;
    background: transparent;
  }
  .problem-card, .environment-grid article, .usecase-grid article, .verifier-table article {
    min-height: 340px;
    padding: 34px;
    border: 1px solid var(--line);
    border-radius: 16px;
    box-shadow: 0 10px 28px rgba(21, 81, 137, .045);
  }
  .problem-card h3 { margin: 70px 0 14px; font-size: 25px; }
  .problem-card p { font-size: 16px; line-height: 1.7; }
  .card-caption { margin-top: 54px; font-size: 12px; font-weight: 600; }

  .execution-layout, .two-col { gap: 120px; }
  .execution-rail { padding-left: 0; }
  .rail-item { padding: 20px 20px 20px 4px; }
  .rail-item > span { width: 38px; height: 38px; }
  .rail-item strong { font-size: 17px; }
  .runtime-explainer { gap: 0; }
  .runtime-feature { padding: 30px 0; }
  .runtime-feature h3 { font-size: 21px; }
  .runtime-feature p { font-size: 16px; line-height: 1.7; }
  .runtime-callout { margin-top: 26px; padding: 26px; border-radius: 14px; }
  .runtime-callout strong { font-size: 15px; }
  .runtime-callout span:last-child { font-size: 15px; }

  .flow-grid { gap: 16px; }
  .flow-card { min-height: 290px; padding: 32px; border-radius: 16px; }
  .flow-card h3 { margin: 58px 0 12px; font-size: 26px; }
  .flow-card p { font-size: 16px; line-height: 1.7; }
  .flow-code { margin-top: 44px; font-size: 12px; }
  .flow-arrow { font-size: 24px; }
  .decision-band { margin-top: 48px; padding: 28px; border-radius: 16px; }
  .decision-pill { border-radius: 999px; }

  .verifier-table article { min-height: 350px; }
  .verifier-table h3 { margin: 64px 0 14px; font-size: 20px; }
  .verifier-table p { font-size: 16px; line-height: 1.7; }
  .verifier-note { margin-top: 20px; padding: 24px; border-radius: 14px; }
  .verifier-note strong { font-size: 16px; }
  .verifier-note p { font-size: 15px; line-height: 1.65; }

  .code-card, .record-card { border-radius: 16px; }
  .code-card-top, .code-card-foot, .record-card-top, .record-card-foot { padding: 16px 20px; font-size: 13px; }
  .code-card pre, .record-card pre { padding: 32px; font-size: 14px; line-height: 1.8; }

  .stack-diagram { max-width: 1060px; }
  .stack-row { gap: 10px; }
  .stack-row span { padding: 12px 16px; border-radius: 999px; font-size: 14px; }
  .stack-arrow { padding: 28px 0; font-size: 13px; }
  .stack-core { padding: 2px; border-radius: 18px; box-shadow: 0 18px 44px rgba(21, 81, 137, .14); }
  .stack-core-label { padding: 18px 22px; border-radius: 16px 16px 0 0; font-size: 13px; }
  .stack-core-grid { border-radius: 0 0 16px 16px; }
  .stack-core-grid > div { min-height: 140px; padding: 30px; }
  .stack-core-grid strong { font-size: 17px; }
  .stack-core-grid small { font-size: 13px; }

  .usecase-header { margin-bottom: 68px; }
  .usecase-grid article { min-height: 280px; }
  .usecase-grid h3 { margin: 52px 0 12px; font-size: 22px; }
  .usecase-grid p { font-size: 16px; line-height: 1.7; }
  .usecase-line { margin-top: 42px; font-size: 12px; }

  .technical-list > div { padding: 24px 0; }
  .technical-list strong { font-size: 16px; }
  .technical-list p { font-size: 15px; line-height: 1.65; }
  .final-section { min-height: 700px; }
  .final-cta { max-width: 920px; }
  .final-cta p { font-size: 18px; line-height: 1.7; }
  .final-meta { font-size: 13px; }
  .footer { min-height: 96px; font-size: 14px; }

  @media (max-width: 1100px) {
    .hero { grid-template-columns: 1fr; gap: 64px; }
    .hero-panel { max-width: 780px; }
    .execution-layout, .two-col { gap: 64px; }
  }

  @media (max-width: 820px) {
    .shell { width: min(calc(100% - 40px), 720px); }
    .nav { min-height: 72px; gap: 12px; }
    .nav-cta { min-height: 40px; padding: 0 15px; font-size: 14px; }
    .hero { min-height: 0; padding-top: 76px; padding-bottom: 96px; }
    h1 { font-size: clamp(3.4rem, 11vw, 5.8rem); }
    h2 { font-size: clamp(2.8rem, 8vw, 4.2rem); }
    .statement-grid, .problem-grid, .flow-grid, .usecase-grid { grid-template-columns: 1fr; }
    .statement-grid > div { min-height: auto; padding: 28px 0; border-bottom: 1px solid var(--line); }
    .statement-grid > div:last-child { border-bottom: 0; }
    .problem-card, .environment-grid article, .usecase-grid article, .verifier-table article { min-height: 0; }
    .flow-arrow { min-height: 28px; }
    .verifier-table, .environment-grid { grid-template-columns: 1fr 1fr; }
    .footer { grid-template-columns: 1fr 1fr; }
  }

  @media (max-width: 560px) {
    .shell { width: calc(100% - 32px); }
    .nav-cta { padding: 0 12px; font-size: 13px; }
    .hero { padding-top: 58px; padding-bottom: 72px; }
    h1 { font-size: clamp(3rem, 14vw, 4.5rem); }
    h2 { font-size: 2.8rem; }
    .hero-lead { font-size: 19px; }
    .hero-actions { flex-direction: column; align-items: stretch; }
    .button { width: 100%; }
    .trace-body { padding-left: 12px; padding-right: 12px; }
    .trace-line { grid-template-columns: 25px 66px 1fr auto; gap: 7px; font-size: 12px; }
    .trace-line b, .trace-line small { font-size: 11px; }
    .verifier-table, .environment-grid { grid-template-columns: 1fr; }
    .code-card pre, .record-card pre { padding: 20px 16px; font-size: 13px; }
    .stack-core-grid { grid-template-columns: 1fr 1fr; }
    .stack-core-grid > div { min-height: 110px; padding: 20px; }
  }

  /* Final palette and alignment pass. */
  :root {
    --federal-blue: #06065d;
    --cobalt-blue: #0e49b5;
    --non-photo-blue: #a2dae0;
    --off-red: #ed0101;
    --turkey-red: #a70107;
    --accent: var(--cobalt-blue);
    --accent-soft: rgba(14, 73, 181, .1);
    --blue: var(--cobalt-blue);
    --text: var(--federal-blue);
    --muted: #465878;
    --muted-2: #6a7891;
    --muted-3: #7c89a0;
    --line: #d9e1ef;
    --line-strong: #b9c9e1;
    --panel: #ffffff;
    --panel-2: #f6f9fe;
    --panel-3: #eaf6f8;
    --paper: #f6f9fe;
    --ink: var(--federal-blue);
    --ink-muted: var(--muted);
  }

  body, button, a, input { font-family: "Geist", ui-sans-serif, system-ui, sans-serif; }
  code, pre, pre code { font-family: "Geist Mono", ui-monospace, monospace; }
  .kicker, .eyebrow, .micro, .card-top, .card-caption, .flow-code,
  .usecase-line, .usecase-tag, .guarantee, .verifier-head, .stack-core-label,
  .stack-row span, .stack-arrow, .decision-title, .decision-pill, .text-link,
  .env-icon, .window-title, .window-state, .mono-label, .feature-number,
  .technical-list span, .record-card-top, .record-card-foot, .code-card-top,
  .code-card-foot, .quickstart-tab, .quickstart-foot, .final-meta, .footer,
  .hero-facts, .hero-panel-caption, .rail-item > span, .rail-item small {
    font-family: "Geist", ui-sans-serif, system-ui, sans-serif;
  }

  .kicker, .eyebrow, .micro { letter-spacing: .04em; text-transform: none; }
  .card-top, .usecase-tag, .verifier-head, .technical-list span { letter-spacing: .04em; }
  .hero-facts { letter-spacing: 0; }

  .window-bar, .trace-footer { background: #e8e8ed; border-color: #c9c9d0; }
  .trace-window { background: #f5f5f7; border-color: #c9c9d0; }
  .trace-body { background: #f5f5f7; }
  .window-dots i { background: none !important; opacity: 1 !important; }
  .window-dots i:nth-child(1) { background: #28c840 !important; }
  .window-dots i:nth-child(2) { background: #ff9500 !important; }
  .window-dots i:nth-child(3) { background: #ffd60a !important; }

  .execution-rail { padding-left: 0; }
  .execution-rail::before {
    left: 22px;
    top: 22px;
    bottom: 22px;
    background: var(--non-photo-blue);
  }
  .rail-item {
    grid-template-columns: 44px 1fr;
    gap: 18px;
    min-height: 86px;
    padding: 20px 34px 20px 0;
    align-items: center;
  }
  .rail-item > span {
    width: 44px;
    height: 44px;
    border: 2px solid var(--cobalt-blue);
    background: #ffffff;
    color: var(--federal-blue);
    font-size: 14px;
    font-weight: 700;
    line-height: 1;
  }
  .rail-item strong { padding-top: 0; font-size: 18px; line-height: 1.2; }
  .rail-item small {
    grid-column: 2;
    margin-top: 0;
    color: var(--muted);
    font-size: 14px;
    line-height: 1.35;
  }
  .rail-item::after {
    content: attr(data-symbol);
    position: absolute;
    right: 0;
    top: 50%;
    color: var(--cobalt-blue);
    font-size: 20px;
    font-weight: 700;
    line-height: 1;
    transform: translateY(-50%);
  }
  .rail-item:nth-child(3)::after,
  .rail-item:nth-child(4)::after,
  .rail-item:nth-child(5)::after { color: var(--off-red); }
  .rail-item.highlight > span { border-color: var(--off-red); color: var(--turkey-red); }

  .rail-item > span, .feature-number, .flow-index, .card-top,
  .env-icon, .verifier-head, .technical-list span, .evidence-points span {
    font-family: "Geist", ui-sans-serif, system-ui, sans-serif;
    font-variant-numeric: tabular-nums;
  }
  .feature-number, .flow-index, .evidence-points span { color: var(--cobalt-blue); font-weight: 700; }
  .env-icon { border-color: var(--non-photo-blue); color: var(--cobalt-blue); }
  .flow-index { font-size: 14px; }
  .card-top, .verifier-head, .technical-list span { font-weight: 650; }

  .problem-card.emphasis-card, .flow-card.active { background: var(--panel-3); border-color: var(--non-photo-blue); }
  .final-mark i { background: var(--cobalt-blue); }
  .text-link, .usecase-tag, .guarantee, .code-card-top span:last-child,
  .record-card-top span:last-child { color: var(--cobalt-blue); }
  .decision-pill { color: var(--federal-blue) !important; border-color: var(--non-photo-blue) !important; }
  .decision-pill.fail, .decision-pill.deny { color: var(--turkey-red) !important; border-color: rgba(167, 1, 7, .28) !important; }
  .decision-pill.pass, .decision-pill.allow { color: var(--cobalt-blue) !important; }
  .code-card, .record-card { box-shadow: 0 18px 42px rgba(6, 6, 93, .08); }
  .stack-core { background: var(--cobalt-blue); }
  .stack-core-label { background: var(--federal-blue); }
  .stack-row span { border-color: var(--non-photo-blue); }
  .runtime-callout { border-color: var(--non-photo-blue); background: var(--panel-3); }
  .callout-mark { border-color: var(--cobalt-blue); color: var(--cobalt-blue); }
  .hero-panel-caption i { background: var(--non-photo-blue); }

  @media (max-width: 560px) {
    .execution-rail::before { left: 20px; }
    .rail-item { grid-template-columns: 40px 1fr; gap: 14px; padding-right: 24px; }
    .rail-item > span { width: 40px; height: 40px; font-size: 13px; }
    .rail-item strong { font-size: 16px; }
    .rail-item small { font-size: 13px; }
    .rail-item::after { right: 0; font-size: 17px; }
  }
</style>


